<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.f.justsharemq.mapper.UserShareMapper">

    <resultMap type="com.f.justsharecommon.entity.UserShare" id="UserShareMap">
        <result property="id" column="id" jdbcType="INTEGER"/>
        <result property="contentId" column="content_id" jdbcType="INTEGER"/>
        <result property="content" column="content" jdbcType="VARCHAR"/>
        <result property="publishRule" column="publish_rule" jdbcType="INTEGER"/>
        <result property="userid" column="userId" jdbcType="INTEGER"/>
        <result property="createTime" column="create_time" jdbcType="TIMESTAMP"/>
        <result property="updateTime" column="update_time" jdbcType="TIMESTAMP"/>
    </resultMap>

    <!--查询单个-->
    <select id="queryById" resultMap="UserShareMap">
        select
id, content_id, content, publish_rule, userId, create_time, update_time
        from user_share
        where  id= #{id}
    </select>

    <!--查询指定行数据-->
    <select id="queryAllByLimit" resultMap="UserShareMap">
        select
id, content_id, content, publish_rule, userId, create_time, update_time
        from user_share
        <where>
            <if test="id != null">
                and id = #{id}
            </if>
            <if test="contentId != null">
                and content_id = #{contentId}
            </if>
            <if test="content != null and content != ''">
                and content = #{content}
            </if>
            <if test="publishRule != null">
                and publish_rule = #{publishRule}
            </if>
            <if test="userid != null">
                and userId = #{userid}
            </if>
            <if test="createTime != null">
                and create_time = #{createTime}
            </if>
            <if test="updateTime != null">
                and update_time = #{updateTime}
            </if>
        </where>
        limit #{pageable.offset}, #{pageable.pageSize}
    </select>

    <!--统计总行数-->
    <select id="count" resultType="java.lang.Long">
        select count(1)
        from user_share
        <where>
            <if test="id != null">
                and id = #{id}
            </if>
            <if test="contentId != null">
                and content_id = #{contentId}
            </if>
            <if test="content != null and content != ''">
                and content = #{content}
            </if>
            <if test="publishRule != null">
                and publish_rule = #{publishRule}
            </if>
            <if test="userid != null">
                and userId = #{userid}
            </if>
            <if test="createTime != null">
                and create_time = #{createTime}
            </if>
            <if test="updateTime != null">
                and update_time = #{updateTime}
            </if>
        </where>
    </select>
    <select id="queryByContentId" resultType="com.f.justsharecommon.entity.UserShare">
        select
            id, content_id,  publish_rule, userId
        from user_share
        where  content_id= #{contentId}
    </select>

    <!--新增所有列-->
    <insert id="insert" keyProperty="" useGeneratedKeys="true">
        insert into user_share(id, content_id, content, publish_rule, userId, create_time, update_time)
        values (#{id}, #{contentId}, #{content}, #{publishRule}, #{userid}, #{createTime}, #{updateTime})
    </insert>

    <insert id="insertBatch" keyProperty="" useGeneratedKeys="true">
        insert into user_share(id, content_id, content, publish_rule, userId, create_time, update_time)
        values
        <foreach collection="entities" item="entity" separator=",">
        (#{entity.id}, #{entity.contentId}, #{entity.content}, #{entity.publishRule}, #{entity.userid}, #{entity.createTime}, #{entity.updateTime})
        </foreach>
    </insert>

    <insert id="insertOrUpdateBatch" keyProperty="" useGeneratedKeys="true">
        insert into user_share(id, content_id, content, publish_rule, userId, create_time, update_time)
        values
        <foreach collection="entities" item="entity" separator=",">
            (#{entity.id}, #{entity.contentId}, #{entity.content}, #{entity.publishRule}, #{entity.userid}, #{entity.createTime}, #{entity.updateTime})
        </foreach>
        on duplicate key update
id = values(id),
content_id = values(content_id),
content = values(content),
publish_rule = values(publish_rule),
userId = values(userId),
create_time = values(create_time),
update_time = values(update_time)
    </insert>

    <!--通过主键修改数据-->
    <update id="update">
        update user_share
        <set>
            <if test="id != null">
                id = #{id},
            </if>
            <if test="contentId != null">
                content_id = #{contentId},
            </if>
            <if test="content != null and content != ''">
                content = #{content},
            </if>
            <if test="publishRule != null">
                publish_rule = #{publishRule},
            </if>
            <if test="userid != null">
                userId = #{userid},
            </if>
            <if test="createTime != null">
                create_time = #{createTime},
            </if>
            <if test="updateTime != null">
                update_time = #{updateTime},
            </if>
        </set>
        where  = #{}
    </update>

    <!--通过主键删除-->
    <delete id="deleteById">
        delete from user_share where  id= #{id}
    </delete>

</mapper>

